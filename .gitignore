node_modules/
dist/
.vercel/
*.local

# extraction working files (diagnostics only, not needed to run the app)
extraction/screenshots/
extraction/contact_sheets/
extraction/__pycache__/

.DS_Store

.vercel
.env*

# agent-tooling reference docs auto-installed by `vercel integration add`
# (Upstash skill docs for coding assistants) - not part of the app itself
.agents/
.claude/
skills-lock.json
